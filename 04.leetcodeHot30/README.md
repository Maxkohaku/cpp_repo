1. 两数之和(vector<int> twoSum(vector<int>& nums, int target))
2. 无重复字符最长子串(int lengthOfLongestSubstring(string s))
3. 最长回文子串(string longestPalindrome(string s))
4. 有效的括号(bool isValid(string s))
5. 两数相加(ListNode* addTwoNumbers(ListNode* l1, ListNode* l2))
6. 合并两个有序链表(ListNode* mergeTwoLists(ListNode* list1, ListNode* list2))
7. 环形链表(bool hasCycle(ListNode *head))
8. 相交链表( ListNode *getIntersectionNode(ListNode *headA, ListNode *headB))
9. 反转链表(ListNode* reverseList(ListNode* head))
10. 盛最多水的容器(int maxArea(vector<int>& height))
11. 三数之和(vector<vector<int>> threeSum(vector<int>& nums))
12. 接雨水(int trap(vector<int>& height))
13. 寻找两个正序数组中位数(double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2))
14. 搜索旋转排序数组(int search(vector<int>& nums, int target))
15. 二分查找(int search(vector<int>& nums, int target))
16. 中序遍历(vector<int> inorderTraversal(TreeNode* root))
17. 层序遍历(vector<vector<int>> levelOrder(TreeNode* root))
18. 最大深度(int maxDepth(TreeNode* root))
19. 最大路径和(int maxPathSum(TreeNode* root))
20. 柱状图最大矩形(int largestRectangleArea(vector<int>& heights))
21. 最小栈(MinStack() void push(int val) void pop() int top() int getMin())
22. 最大子数组和(int maxSubArray(vector<int>& nums))
23. 爬楼梯(int climbStairs(int n))
24. 买卖股票最佳时机(int maxProfit(vector<int>& prices))
25. 合并区间(vector<vector<int>> merge(vector<vector<int>>& intervals))
26. 螺旋矩阵(vector<int> spiralOrder(vector<vector<int>>& matrix))
27. 全排列(vector<vector<int>> permute(vector<int>& nums))
28. 子集(vector<vector<int>> subsets(vector<int>& nums))
29. 岛屿数量(int numIslands(vector<vector<char>>& grid))
30. 课程表(bool canFinish(int numCourses, vector<vector<int>>& prerequisites))